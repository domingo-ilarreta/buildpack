#!/usr/bin/env bash

# Este archivo contiene los pasos de compilación y configuración para tu aplicación.

# Asegúrate de tener instaladas las dependencias necesarias aquí.
# Por ejemplo, para instalar las dependencias de capybara-webkit:
apt-get update
apt-get install -y qt5-default libqt5webkit5-dev gstreamer1.0-plugins-base gstreamer1.0-tools gstreamer1.0-x

# Continúa con los pasos de compilación de tu aplicación.
# Por ejemplo, ejecuta el comando `bundle install`:
bundle install
